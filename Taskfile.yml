version: '3'

vars:
  OUTPUT_FILE: dist/web

env:
  CGO_ENABLED: 0

dotenv: ['.env', '{{.ENV}}/.env', '{{.HOME}}/.env']

tasks:
  build-frontend:
    cmds:
      - pnpm build
    sources:
      - "**/*.html"
      - "**/*.js"
      - "**/*.ts"
      - "**/*.css"

  build-web:
    deps: [build-frontend]
    cmds:
      - go build -o {{.OUTPUT_FILE}} ./cmd/web/main.go
    sources:
      - "**/*.go"
    generates:
      - "{{.OUTPUT_FILE}}"

  run:
    deps: [build-web]
    cmds:
      - "./{{.OUTPUT_FILE}}"
  
  dev:
    deps: [build]
    cmds:
      - "./{{.OUTPUT_FILE}}"
    watch: true 
    sources:
      - "**/*.go"
      - "**/*.css"
      - "**/*.html"
